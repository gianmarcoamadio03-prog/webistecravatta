# Scraper environment
#
# Metti qui le variabili in un file .env (IGNORATO da git) oppure in CI.

# ID dello spreadsheet (non il link completo)
GOOGLE_SHEETS_ID="YOUR_SHEET_ID"

# Path al file service-account.json (NON committare)
GOOGLE_SERVICE_ACCOUNT_JSON="./service-account.json"

# (Opzionale) specifica il browser (Playwright)
# PLAYWRIGHT_BROWSERS_PATH=0
